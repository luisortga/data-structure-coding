// Balas trazadoras, inspiración en el libro el programador pragmatico

class Trazo {

    /** @type {string} */
    #trace = ""
    way = ""
    map = ""
    count = 0


    constructor(way, map, count) {
        /** @type {string} */
        this.way = way
        /** @type {string} */
        this.map = map
        /** @type {number} */
        this.count = count
    }

    compuesto(rule) {
        let demo = this.way
        rule = ""
        if (rule === '16') {
            
        }
    }


}